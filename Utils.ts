//isme bhi namespace create karenge with same name:

namespace UsersUtils{
    export class Parent{
        name;     //we are taking name by default undefined
        setName(name)
        {
            return this.name=name;
        }

    }

    //interface
    export interface userType{
        getName(): 
    }

}